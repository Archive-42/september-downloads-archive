export enum ChatOrientation {
	Right = 'right',
	Left = 'left',
}

export enum ChatPosition {
	Auto = 'auto',
	Relative = 'relative',
	Fixed = 'fixed',
}

export enum ButtonStyle {
	Bubble = 'bubble',
	Greeting = 'greeting',
}
